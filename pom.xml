<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>at.postkorb</groupId>
  <artifactId>postkorb-schnittstelle</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>USP Postkorb Schnittstelle</name>
  <description>Automatische Abholung von Dokumenten aus "Mein Postkorb" (USP) unter Windows</description>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <junit.version>5.11.4</junit.version>
    <jaxws.version>4.0.3</jaxws.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.2</version>
        <configuration>
          <!-- Umlaute in Dateinamen auch auf Build-Servern mit POSIX-Locale -->
          <environmentVariables>
            <LC_ALL>C.UTF-8</LC_ALL>
          </environmentVariables>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>shade</goal></goals>
            <configuration>
              <finalName>postkorb-schnittstelle</finalName>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>at.postkorb.Main</mainClass>
                </transformer>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Wird automatisch aktiv, sobald die WSDL aus dem USP-Maven-Projekt
      nach src/main/wsdl kopiert wurde. Generiert dann den SOAP-Client (JAX-WS).
    -->
    <profile>
      <id>zuse-aa</id>
      <activation>
        <file>
          <exists>${basedir}/src/main/wsdl/zuseaa_p2.wsdl</exists>
        </file>
      </activation>
      <dependencies>
        <dependency>
          <groupId>com.sun.xml.ws</groupId>
          <artifactId>jaxws-rt</artifactId>
          <version>${jaxws.version}</version>
        </dependency>
      </dependencies>
      <build>
        <plugins>
          <plugin>
            <groupId>com.sun.xml.ws</groupId>
            <artifactId>jaxws-maven-plugin</artifactId>
            <version>${jaxws.version}</version>
            <executions>
              <execution>
                <goals><goal>wsimport</goal></goals>
                <configuration>
                  <wsdlDirectory>${basedir}/src/main/wsdl</wsdlDirectory>
                  <wsdlFiles><wsdlFile>zuseaa_p2.wsdl</wsdlFile></wsdlFiles>
                  <packageName>at.postkorb.zuseaa.generated</packageName>
                  <wsdlLocation>/wsdl/zuseaa_p2.wsdl</wsdlLocation>
                  <extension>true</extension>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
        <resources>
          <resource>
            <directory>src/main/resources</directory>
          </resource>
          <resource>
            <directory>src/main/wsdl</directory>
            <targetPath>wsdl</targetPath>
          </resource>
        </resources>
      </build>
    </profile>
  </profiles>
</project>
